pipeline {
    agent any
    stages {
        stage('Init') {
            steps {
                echo 'Init'
            }
        }

        stage('Continue') {
            steps {
                echo 'Continue'
            }
        }

        stage('End') {
            steps {
                echo 'End'
            }
        }
    }
}